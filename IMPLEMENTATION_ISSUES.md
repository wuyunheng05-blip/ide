# Workmate AI 工作区：实现问题与解决记录

**截止日期：** 2026-08-28  
**范围：** 项目/会话管理、受管理工作区、AI Agent Harness、受限验证、写入审批与任务计划。

| 问题 | 原因 | 解决方式 | 当前状态 |
| --- | --- | --- | --- |
| 项目会话被显示为独立目录 | 会话列表按类型全局渲染，未绑定项目树 | 改为在每个项目下缩进渲染其 `sessions`，切换时同步项目与会话 | 已完成 |
| 项目操作按钮常驻且层级无法折叠 | 项目行没有独立折叠状态和悬停规则 | 增加 `collapsedProjectIds`，并用 `.project-row:hover` 控制按钮可见性 | 已完成 |
| 删除最后一个项目没有反馈 | 原逻辑禁止删除最后项目，且没有替代工作区 | 主进程创建 `%APPDATA%\Workmate\workspaces\<UUID>\workspace`，替换为新项目 | 已完成，待桌面验收 |
| 首次启动无有效项目 | 受管理工作区只在删除最后项目时创建 | 初始化阶段检测无效/空项目记录并调用 `workspace:create-managed`；用 ref 防止重复创建 | 已完成，待桌面验收 |
| AI 可写文件但用户缺少逐次控制 | 自动写入直接落盘 | 增加设置开关；主进程生成有限 Diff 摘要并用原生确认框审批 | 已完成 |
| AI 缺少验证闭环 | 工具没有安全的命令执行能力 | 仅允许 `npm run build/test/lint`；检查脚本存在、工作区 cwd、无 shell、120 秒超时、24 KB 输出限制 | 已完成 |
| 计划步骤被“步骤 N”覆盖 | 执行状态事件未保存计划原文 | `callDeepSeek` 保存 `planSteps`，通过 `emitPlanStatus` 始终使用原始标题 | 已完成 |
| 工具异常会触发 `isPlanSubmission` 作用域错误 | 变量在 `try` 的块级作用域内声明，但 `catch` 访问它 | 在 `try` 前声明 `let isPlanSubmission = false`，之后赋值 | 已完成 |
| 计划和执行日志混在一起，完成数不准确 | UI 以单一活动列表渲染和计数 | 依据 `kind === 'plan'` 分为“任务计划”和“执行过程”，分别统计 | 已完成 |
| 待执行步骤不明显 | 缺少 `pending` 对应 CSS | 新增虚线边框与灰色状态图标样式 | 已完成 |
| 计划功能未做真实桌面端流程验证 | 当前只执行了静态诊断与构建 | 已提供人工测试用例；需用真实 API Key、桌面 Electron 和本地项目验收 | 待验收 |

## 关键约束与经验

1. **安全边界必须在 Electron 主进程执行。** 渲染进程的开关或提示只能改善体验，不能作为写文件、运行命令或路径限制的唯一保障。
2. **AI 的系统提示不等于强制执行。** 当前计划工具由系统提示要求优先调用；若需要绝对保证，应在工具调度层拒绝未提交计划的非简单任务操作。
3. **状态事件需可覆盖且保存原始元数据。** 计划条目按稳定 `id` 更新，且状态转换不能丢失最初的标题、说明和类型。
4. **构建通过不替代桌面验收。** `npm run build` 已验证 TypeScript/Vite/资源路径；IPC、原生确认框、应用数据目录和真实模型工具调用仍需在 Electron 中手动验证。
## 2026-08-29 计划前置强制校验

### 为什么要这样改？
- 项目 Agent 的“先提交计划”此前只由 system prompt 约束，模型可能忽略提示后直接调用写文件、创建目录或运行验证命令。
- 将规则放到 Electron 主进程的工具调度层，可让计划成为高风险操作前的可见检查点，并让 UI 能稳定展示执行步骤。

### 不改有什么问题？
- 模型可能在未说明执行意图和步骤时直接修改文件或启动验证，用户无法在操作前判断任务范围。
- 安全边界会依赖模型是否遵守自然语言指令，导致不同模型、提示词或上下文长度下的行为不稳定。
- 后续增加取消、审批、审计和步骤级失败处理时缺少可靠的任务状态基础。

### 改动范围与验证
- 文件：`electron/security.mjs`、`electron/main.mjs`、`test/security.test.mjs`。
- 行为：项目会话未提交 `submit_plan` 时，主进程拒绝 `write_file`、`create_directory`、`run_verification`；读取、搜索和只读 Git 操作仍可执行。
- 验证：`npm test` 已通过 5 项测试，其中包含计划前置规则；`npm run build` 已通过。

## 2026-08-29 任务取消与执行记录持久化

### 为什么要这样改？
- Agent 任务可能包含流式模型调用、联网搜索或受限验证命令；用户需要在发现任务方向不对、等待时间过长或切换工作时主动停止。
- 计划和工具活动原本仅存于运行时内存，刷新或重启后无法复盘本次任务做过哪些读取、写入和验证。
- 通过会话键维护主进程 `AbortController`，并将活动日志保存到浏览器本地存储，可在不扩大 IPC 权限的前提下提供最小可控性与可追踪性。

### 不改有什么问题？
- 用户只能等待任务、网络请求或验证命令自然结束，无法降低错误任务的时间与资源消耗。
- 前端即使停止监听也无法真正停止主进程中的网络请求或子进程，容易造成界面与实际执行状态不一致。
- 重启应用后执行计划和工具结果消失，排查 AI 修改原因或验证失败原因时缺少依据。

### 改动范围与验证
- 文件：`electron/main.mjs`、`electron/preload.cjs`、`src/electron.d.ts`、`src/App.tsx`、`src/styles.css`。
- 行为：新增 `ai:cancel` IPC；按会话取消 DeepSeek/联网请求和受限 npm 验证子进程；取消后仍保留已完成活动，并将未完成活动标记为“任务已取消”。取消不会回滚已经完成的文件写入或目录创建。
- 持久化：活动日志保存于浏览器本地存储，按会话键恢复；不保存 API 密钥或工作区文件内容。
- 验证：`npm test` 已通过 5 项测试；`npm run build` 已通过；相关文件静态诊断无问题。
