# Workmate AI 工作区：项目状态

**更新日期：** 2026-08-28  
**当前版本：** `0.1.5`  
**验证状态：** 已执行 `npm test`（4 项安全单元测试）与 `npm run build`，TypeScript 检查和生产构建通过。

## 已完成

### 项目与会话管理
- [x] 创建、选择、重命名项目；项目列表按最近更新时间排序。
- [x] 项目会话以树状层级显示在所属项目下方，不再使用单独的“项目会话”列表。
- [x] 每个项目行支持展开/收起下级会话；展开箭头、新建会话、重命名和删除图标仅在鼠标悬停项目行时显示。
- [x] 从项目行的新建图标创建会话，并自动切换到该项目和新会话。
- [x] 项目会话与独立会话均支持创建、切换、重命名、删除。
- [x] 删除项目会话时同时删除其关联的需求记录；每个项目至少保留一个会话。
- [x] 删除项目时会要求确认；删除普通项目仅删除应用内项目、会话、需求记录，不会删除该项目磁盘目录。
- [x] 删除当前项目后，自动切换到最近更新的剩余项目。

### 最后一个项目与受管理工作区
- [x] 可以删除最后一个项目。
- [x] 删除最后一个项目并确认后，由 Electron 主进程创建受管理工作区目录：`%APPDATA%\Workmate\workspaces\<UUID>\workspace`。
- [x] UUID 使用 Node.js `randomUUID()` 生成；目录由主进程递归创建。
- [x] 新目录会设为当前 Electron 工作区，并注册为新的项目，包含默认“工程启动”会话。
- [x] 替换为受管理工作区时，编辑器标签、草稿和已保存状态会被清空，文件树切换至新目录。
- [x] 通过安全 IPC `workspace:create-managed` 从渲染进程调用主进程创建目录；preload 和 TypeScript 声明已同步。

### 工作区与文件
- [x] 可通过系统目录选择器打开本地项目。
- [x] 支持目录树展示、打开文件、编辑、保存、刷新文件树。
- [x] 主进程限制读写在当前工作区内，拒绝工作区外路径；限制文本文件最大 5 MB，并拒绝二进制文件。
- [x] 支持工作区文本搜索：跳过依赖、Git、构建缓存、二进制及超过 5 MB 的文件，最多返回 50 个命中；界面可直接打开命中文件。
- [x] 支持只读 Git 状态与未暂存差异面板；命令以无 shell 子进程运行，限制工作区目录、120 秒超时并截断输出。

### 对话、AI 与需求
- [x] 项目会话与独立会话分离管理；会话数据及活动选择保存在浏览器 `localStorage`。
- [x] 支持 DeepSeek 设置、安全存储、流式输出、执行进度和活动记录；活动记录按会话保存于本地存储，可在重启后查看。
- [x] 进行中的 Agent 任务支持取消：主进程会中止模型/联网请求及受限验证子进程；已完成的文件写入不会自动回滚。
- [x] 项目 Agent 对分析、修改、创建、修复或验证任务会先提交 2–6 步任务计划；计划与工具执行过程分区显示，并展示 `pending / running / done / error` 状态。
- [x] 独立会话可使用豆包搜索 Global；项目会话不使用联网搜索。
- [x] 项目对话任务会自动创建需求，可在需求看板更新“待处理 / 进行中 / 已完成”状态。
- [x] 支持搜索独立会话和所有项目会话，并可跳转到对应项目与会话。

### 构建与发布
- [x] 可选启用逐文件写入确认：每次 AI 写入前显示相对路径、行数/体积变化和有限 Diff 预览，拒绝时不会落盘。
- [x] 可选启用受限验证命令：AI 仅可在当前工作区运行 package.json 已定义的 `npm run build`、`npm run test` 或 `npm run lint`；默认关闭，执行无 shell、最长 120 秒且输出限制为 24 KB。
- [x] Vite + React + TypeScript 生产构建。
- [x] Electron Windows portable 打包脚本已配置：`npm run package:win`。
- [x] 最近一次 `npm run build` 已通过。

## 已实现，但需要人工验收

- [ ] 在桌面版中实际删除最后一个项目，确认生成目录与预期完全一致：`%APPDATA%\Workmate\workspaces\<UUID>\workspace`。
- [ ] 验证新受管理目录创建后，可在其中创建、保存并由 AI 修改文件。
- [ ] 验证删除当前项目、非当前项目、以及收起项目后执行新增/重命名/删除图标的交互体验。
- [ ] 验证升级或已有旧版 localStorage 数据时，项目和会话恢复行为符合预期。
- [ ] 发送项目修改或验证任务，确认 Agent 先展示任务计划，并在工具执行时按 `pending → running → done/error` 更新步骤；确认简单问答与独立会话不显示项目计划。
- [ ] 打包后在干净的 Windows 环境运行，验证 Electron IPC、系统文件选择与应用数据目录权限。

## 未完成 / 待确认

- [x] 首次安装或启动且不存在有效项目记录时，自动创建受管理工作区并将其设为默认项目。
- [ ] **受管理工作区的项目名称待确认。** 当前目录和项目名称为 `workspace`；如需显示为“默认工作区”或用户可编辑的名称，需要补充命名策略。
- [ ] **受管理工作区的清理策略待确认。** 删除其项目记录不会删除磁盘目录；是否需要在设置中提供“删除工作区目录”的高风险操作尚未实现。
- [ ] **项目删除提示待优化。** 当前使用浏览器确认框；可替换为应用内确认弹窗，明确区分“删除应用记录”与“保留磁盘文件”。
- [ ] **无项目状态未实现。** 当前通过创建受管理工作区保证至少有一个项目；若未来允许删除后不自动创建，需要新增空状态页面和引导流程。
- [x] 最小自动化安全测试已配置：使用 Node 内置 `node:test` 覆盖工作区路径边界、搜索参数限制和验证命令白名单；`npm test` 已通过。
- [ ] 项目/会话删除、IPC 创建目录、写入审批、计划状态迁移等自动化测试尚未覆盖。
- [ ] **应用版本号尚未更新。** 功能已超过初始 `0.1.5` 行为，但 `package.json` 与界面版本仍为 `0.1.5`。

## 关键文件

| 文件 | 职责 |
| --- | --- |
| `src/App.tsx` | 项目、会话、需求、工作区和界面状态管理 |
| `src/styles.css` | 项目树、悬停操作图标、会话层级等样式 |
| `electron/main.mjs` | 工作区文件系统、AI 调用与 `workspace:create-managed` IPC |
| `electron/preload.cjs` | 安全暴露 `window.workspace.createManaged()` 等 IPC API |
| `src/electron.d.ts` | 渲染进程 Electron API 类型声明 |
| `package.json` | 构建、桌面启动和 Windows 打包脚本 |

## 常用验证命令

```powershell
npm run build
npm run desktop:production
npm run package:win
```

## 下一步开发建议（按优先级）

### 1. 完成桌面端人工验收
- [ ] 按本文“已实现，但需要人工验收”逐项验证受管理工作区、项目树、写入审批、任务计划和受限验证命令。
- [ ] 对计划功能执行一次完整用例：读取项目、创建/修改文件、读取确认、可选 `npm run build`；确认计划步骤和执行日志状态正确。

### 2. 强化 Agent 执行体验
- [x] 项目会话对写入、创建目录和验证操作已在主进程强制要求先提交任务计划。
- [x] 支持取消当前 Agent 任务，并持久化每个会话最近一次的计划和执行活动。
- [ ] 在一次任务中为多份计划提供分组或历史保留，避免连续任务的执行痕迹混淆。
- [ ] 为计划提供“执行完成”摘要，以及可选的折叠/展开能力。

### 3. 增补受控开发能力
- [x] 已提供工作区代码搜索，以及无 shell、只读的 `git status` / 未暂存 `git diff`；Git 命令具备路径边界、超时和输出截断。
- [ ] 如需更广义终端能力，仍应采用严格白名单，并为每条命令定义参数校验、路径限制、超时、输出截断与审批。
- [ ] 增加项目规则加载能力，例如读取工作区内 `AGENTS.md` 或 `.kiro/steering`，并将规则注入项目 Agent 上下文。

### 4. 质量与发布
- [ ] 配置自动化测试，优先覆盖路径边界、受限命令、写入拒绝、计划状态迁移和受管理工作区创建。
- [ ] 完成人工验收后更新版本号、变更日志，并重新生成 Windows portable 安装包。
